{
  "name": "asikar-firestore-rules-tests",
  "private": true,
  "version": "1.0.0",
  "description": "Firestore güvenlik kuralı testleri (çağrı kapma yarışı + zaman aşımı)",
  "scripts": {
    "test": "mocha \"*.test.js\" --timeout 10000"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.3.0",
    "mocha": "^11.1.0"
  }
}
