---
katman: Ön Yüz
durum: onaylı
mvp_kritik: evet
bağımlılıklar: [[04-Auth]], [[03-Veritabani]], [[02-API-Arka-Uc]], [[10-Cache-CDN]]
---

# 01 · Ön Yüz (Frontend)

## Neden önemli
Ön yüz ürünün tek temas noktası; engelli kullanıcı için erişilebilirlik burada ya vardır ya yoktur, ortası yoktur. Atlanırsa (kötü tasarlanırsa) görme engelli kullanıcı "YARDIM İSTE" butonunu bulamaz ve ürünün varlık sebebi çöker.

## Karar (ne + NEDEN)
**Ne:** **Flutter (Dart, SDK ^3.9.2)** ile tek kod tabanlı, Android + iOS native + Web çıktısı.

**Neden Flutter:**
- Tek kod tabanı → küçük ekibin iki platforma da yetişmesinin tek gerçekçi yolu (kısıtlı ekip/zaman).
- Native erişim: kamera, konum, push, CallKit — hepsi olgun paketlerle mevcut.
- `Semantics` widget'ı ile erişilebilirlik birinci sınıf; ekran okuyucu desteği zahmetsiz.

**State yönetimi:** **Riverpod (flutter_riverpod ^2.5.1)** — `settingsServiceProvider` `ProviderScope.overrides` ile inject edilir; async başlatılan `SettingsService` test edilebilir şekilde enjekte edilir. **Neden Riverpod:** compile-time güvenli, BuildContext'e bağımsız, global ayar (tema/erişilebilirlik) için ideal.

**Routing:** Merkezi `AppRouter` + `AppRoutes` sabitleri, `onGenerateRoute`. **Kural:** String literal route yazılmaz, hep `AppRoutes` sabiti. **Neden:** Kayıt akışı 8 ekran zinciri; tek yerden yönetilmezse parametre geçişi (userType, interests) hata kaynağı olur.
- **Durum (O3 — *2026-06-29*):** Önceden `AppRouter` tanımlıydı ama hiç `pushNamed` kullanılmıyordu (tüm gezinme elle `MaterialPageRoute` ile → merkezi router ölü koddu). Denetimle düzeltildi: tüm geçişler `pushNamed`/`pushReplacementNamed`/`pushNamedAndRemoveUntil`'e taşındı; kayıt zinciri argümanları (`userType:String`, `selectedInterests:Set<String>`, `isVolunteer:bool`) ve `routeScreen` (`destinationLocation:LatLng`) AppRouter case'leriyle birebir; iki eksik route eklendi (`addVenue`, `venueDetail{venueId}`). `flutter analyze` temiz, 62 test yeşil. **Bilinçli istisna:** **CallScreen** `navigatorKey` + 15-deneme retry + `pendingCallId` fallback ile `MaterialPageRoute` üzerinden push edilir (bkz. Zorunlu Kalıplar) — `AppRoutes`'a alınmadı; bu kritik çağrı akışı kuralı bilinçli atlar.

**Tema:** Dinamik — yüksek kontrast × karanlık mod = 4 kombinasyon. Yüksek kontrast karanlıkta `sarı üzerine siyah` (0xFFFFFF00), açıkta `koyu lacivert üzerine beyaz` (0xFF000080). **Kural:** Inline `Color(...)` yasak; hep `AppColors`.

**Erişilebilirlik mekanikleri:**
- Yazı boyutu uygulama içi `TextScaler.linear(fontScale)` ile (0.8x–1.4x); sistem ayarından bağımsız.
- Kritik widget'larda `Semantics(label: ...)` (YARDIM İSTE, rota butonları, çağrı sonlandır). Haritadaki `MapActionButton` (Yol Tarifi/Ara/Paylaş) buton rolü + etiket taşır, iç ikon+metin `ExcludeSemantics` ile çift okunmaz (*2026-06-29*, O10; widget testli — bkz. [[07-CI-CD]]).

## MVP Kapsamı
**VAR:**
- 3 sekmeli `IndexedStack` navigasyon (Topluluk / Ana Sayfa / Harita)
- **Topluluk sekmesi** (`community_screen.dart`): kullanıcı katkılı `venues` mekânlarının keşfi — realtime `venuesStreamProvider` + **client-side** arama, kategori ve **erişilebilirlik seviyesi** filtresi (`filteredVenuesProvider`). PRD'deki "Sakin/Turist mekân keşfi" akışının UI'ı (bkz. [[PRD]], [[03-Veritabani]])
- Kullanıcı tipine göre dinamik ana sayfa (Disabled/Volunteer/Standard)
- `AccessibilityDrawer` (yazı, kontrast, ses, karanlık mod)
- Harita ekranı (`map_screen.dart`, flutter_map + vektör/raster karo): **hibrit POI** (Overpass + Foursquare paralel, Foursquare-öncelikli birleştirme — bkz. [[10-Cache-CDN]]), erişilebilirlik katmanları (footway/tekerlekli sandalye yolları, hissedilebilir yüzey + asansör + **engelli otoparkı** node'ları), kategori filtresi
  - **Google tarzı görünüm — vektör taban haritası (*2026-07-04*):** Şikayet: harita renk paleti/görünümü Google Haritalar gibi olsun. Raster PNG karolarda renkler görüntünün **içine gömülü** olduğundan (`AppColors` ile değiştirilemez), "Google rengi" istemek pratikte **karo kaynağını değiştirmek**tir. Ücretsiz/anahtarsız dünyada en Google-benzeri raster olan CartoDB Voyager zaten kullanılıyordu → gerçek iyileşme için **vektör karo**ya geçildi: **"Varsayılan" harita türü artık OpenFreeMap Liberty VEKTÖR stiliyle** çizilir (`vector_map_tiles` ^9.0.0-beta.8 · su/park/yol renkleri **stil dosyasından** gelir → Google paletine çok yakın, yazılar keskin). **Neden bu paket/sürüm:** 9.0.0-beta.8 `flutter_map 8.x`'i destekler **ve** `flutter_gpu`'ya bağımlı DEĞİL (10.x aksine — deneysel Impeller gerektirir); pub.dev production için bunu önerir. **Neden OpenFreeMap:** kalıcı **$0 + anahtarsız** (bilinçli tercih — bkz. [[09-Rate-Limiting]]); Google'ın kendi karolarını anahtarsız çekmek ToS ihlali (vault Google Places'i zaten lisans/maliyetle elemişti — bkz. [[03-Veritabani]]). **Kalıp:** stil `initState`'te `StyleReader` ile **async** yüklenir; yüklenene ya da **yüklenemezse** (OpenFreeMap erişilemez) **CartoDB Voyager RASTER fallback** gösterilir (`_vectorStyle == null` → `TileLayer`) → harita asla boş kalmaz. **Uydu/Arazi modları raster kalır** (bilinçli — Google da uyduyu raster gösterir). POI declutter/marker sistemi (aşağıda O13) **değişmedi** — marker'lar tabandan bağımsız üst katman; `camera.latLngToScreenOffset` projeksiyonu karo tipinden etkilenmez. **Atıf:** OpenFreeMap = OpenStreetMap verisi + OpenMapTiles şeması → vektör taban aktifken köşe rozetine **"© OpenMapTiles"** OSM atfının yanında eklenir (`MapAttributionBadge.showOpenMapTiles`, tek kaynak `map/map_attribution.dart`, widget testli — bkz. [[10-Cache-CDN]], [[07-CI-CD]]). Karo cache/kota etkisi: [[10-Cache-CDN]] · "Temel harita karo cache'i". ⚠️ **Bilinçli sınır:** OpenFreeMap bağışla dönen ücretsiz kamu servisi; Türkiye geneli yoğunlukta yavaşlarsa aynı Liberty stili self-host edilebilir (fallback zaten raster'a düşürür).
    - **Vektör POI etiketleri gizlendi → hepsi tıklanabilir POI oldu (*2026-07-05*):** Liberty vektör karoları OpenMapTiles'ın kendi `poi` katmanını içeriyordu (eczane/market ikon+isimleri karoya çizili) → (1) bizim POI pin'lerimizle **çakışıyordu**, (2) boyalı grafik olduğu için **tıklanamıyordu** (`vector_map_tiles` render edilen etikete hit-test sunmaz). Çözüm iki parçalı: **(a) Vektör `poi` katmanı gizlendi** — `_loadVectorStyle` stil JSON'ından `source-layer=='poi'` katmanlarını çıkarıp temayı `vtr.ThemeReader` ile yeniden kurar (`_vectorTheme`); sokak/mahalle/su adları KALIR, yalnız tekil POI etiketleri çıkar. **(b) O mekanlar kendi TIKLANABILIR katmanımıza taşındı** — yeni `OmtPoiService` (`lib/services/omt_poi_service.dart`) aynı OpenMapTiles karolarını (`VectorTileProvider.provide` ile — harita ile AYNI kaynak, ekstra servis/URL yok) çekip MVT'yi ayrıştırır (`vector_tile` decode, `compute` ile arka planda), `poi` feature'larını `OsmPoi`'ye (`osmType='omt'`) çevirir → mevcut declutter/marker/`OsmPoiSheet` sistemine **4. kaynak** olur (Overpass + canlı Foursquare + FSQ taban + **OMT**). Böylece **haritada görünen her mekan tıklanınca detay paneli açar** ve diğer kaynakların kaçırdığı yerler (yalnız OSM'de olan bir eczane) de POI olur — kullanıcı isteği: "vektörden gelen mekanlar da POI olsun". **Neden bu yol (parite):** OpenMapTiles ve Overpass ikisi de OSM türevi ama bizim varsayılan çekimimiz kota için 7 kategoriyle sınırlı; aynı karodan okumak **birebir pariteyi** garanti eder (kategori setini genişletip Overpass kotasını şişirmeden). Saf/testli parçalar: `omtTilesForBounds` (kutu→tile) + `omtRawType` (class/subclass→kategori; bilinmeyen→"Mekan", yine gösterilir) → `test/unit/omt_poi_parser_test.dart`. Dedup: `MapVisuals.mergePois(_osmPois, omtPois)` — OMT boşluk doldurucu, mevcut (daha zengin: telefon/saat/wheelchair) kayıt öncelikli. Atıf: OMT=OSM verisi → `isFoursquare=false` → sheet'te OSM atfı; köşe rozetinde zaten OSM+OpenMapTiles. Karo çekim/cache: [[10-Cache-CDN]] · "Temel harita karo cache'i". ⚠️ **Sınır:** yalnız isimli poi feature'ları alınır (isimsiz kaldırım mobilyası gürültüsü elenir); OMT poi verisi seyrek (isim+kategori; telefon/saat yok) → gap-filler rolü.
  - **Erişilebilirlik katmanları tek yerde — harita filtreleme modalı (*2026-07-02*):** Erişilebilirlik katmanları yalnızca **katman seçici modalında** (`_showLayerPicker` → `MapOverlayChip`) toggle edilir: Hissedilebilir Yüzey · Tekerlekli Sandalye · Asansör · **Engelli Otoparkı** (+ Toplu Taşıma/Bisiklet/Yürüyüş). Aktif katmanlar `_fetchOverpassLayer` ile Overpass'tan çekilip polyline/marker olarak çizilir (`_showTactile/_showWheelchair/_showElevator/_showParking` bayrakları). Engelli otoparkı `amenity=parking|parking_space` + `wheelchair=yes|designated` node'ları (indigo `AppColors.mapParking`, `Icons.local_parking`); marker sınıflandırmasında otopark kontrolü wheelchair'dan **önce** gelir (aksi hâlde otopark node'u tekerlekli sandalye markerı gibi görünürdü). Sorgu üretimi saf/birim testli: `accessibilityOverpassQuery(..., parking:)` (bkz. [[10-Cache-CDN]], [[07-CI-CD]]).
    - **Kaldırılan tekrar:** Arama çubuğu açıkken altında gösterilen erişilebilirlik filtre çipleri (`MapFilterChip` → Tekerlekli Sandalye/Hissedilebilir Yüzey/Asansör/Engelli Otoparkı) kaldırıldı — aynı katmanlar zaten harita filtreleme modalında yer alıyordu ve arama akışına bir etkileri yoktu (yalnızca `_activeFilter` state'ini değiştiriyorlardı). `MapFilterChip` widget'ı + widget testi silindi; arama overlay'i yalnızca sonuç listesi gösterir.
  - **İlk yükleme hızı (O11 — *2026-06-29*):** Açılışta mekanlar artık `onMapReady` ile **otomatik ve debounce'suz** çekilir (`initialZoom` 15); önceden yalnızca kullanıcı haritayı oynatınca tetiklendiği için "mekanlar çok geç yükleniyor" hissi vardı. Filtre yokken sorgu 21/12 kategori yerine 7 sık kategoriyle sınırlanır; "Mekanlar yükleniyor..." göstergesi iki kaynağı ayrı izleyerek (FSQ key'i boşsa bile) takılmaz. Nedenler + cache gerekçesi: [[10-Cache-CDN]] · "İlk yükleme gecikmesi".
  - **Açılışta kullanıcı konumu (*2026-07-02*):** Harita önce Sakarya merkezinde (`_sakaryaCenter`) açılır; kullanıcı **daha önce (bir kerelik) konum iznini verdiyse** açılışta **tekrar izin sormadan** konumuna ortalanır. `_initLocationOnStart` önce hızlı **son bilinen konum** (`getLastKnownPosition`, cache'li → anında) sonra **güncel konum** (`getCurrentPosition`) ile tazeler; harita henüz hazır değilse `onMapReady` `_currentLocation`'a taşır. İzin verilmemişse **hiç istenmez** (Sakarya merkezi fallback kalır) — sistem izin diyaloğu yalnızca "Konumum" butonunda (`_getCurrentLocation`) çıkar. **Gizlilik:** açılışta izin dayatılmaz, konum loglanmaz (KVKK — bkz. [[08-Guvenlik]]).
    - **Yumuşak geçiş:** Konuma ortalanma ani sıçrama değil **kayan geçiş** (`_animatedMapMove` · `Tween` + `AnimationController`, easeInOut 700ms) — flutter_map yerleşik animasyon sunmadığı için harici bağımlılık eklemeden yazıldı. Üst üste binen geçişler iptal edilir (son bilinen → güncel konum arası titremesin); POI çekimi geçiş **bitince** (`onFinished` → `_fetchPoisAfterMove`) yapılır, böylece debounce/cache/kota mantığı korunur (bkz. [[10-Cache-CDN]], [[11-Olcekleme]]). Aynı yardımcı "Konumum" butonunda da kullanılır (tutarlılık).
  - **Marker yoğunluğu — Google tarzı kademeli görünürlük (O13 — *2026-07-01*):** Eski hâlde tüm POI'ler her zoom'da aynı anda ikon+isimle çiziliyordu → isimler üst üste binip haritayı örtüyordu. Google Haritalar gibi **kademeli görünürlük**e geçildi: aynı zoom'da yalnızca öne çıkan mekanlar isimle, bir kısmı küçük **nokta**, gerisi **gizli**; yaklaştıkça daha fazlası isme yükselir. İki saf/birim-testli katman:
    - **Öncelik + zoom eşikleri** (`map/poi_priority.dart`): `poiPriority` kategori → önem ağırlığı (3 yüksek: hastane/eczane/otel/market/benzin/ibadet/üniversite/müze/park/sinema; 2 orta: restoran/kafe/banka/okul; 1 düşük: küçük dükkanlar). `poiLabelMinZoom`/`poiDotMinZoom` her önceliğe **isim** ve **nokta** için ayrı en-düşük-zoom verir → kademeli görünürlük: **isim** 3→z15, 2→z17, 1→z18; **nokta** 3→yok (düşük zoom'da nokta kalabalığı olmasın), 2→z16, 1→z17. Yani z15'te yalnız yüksek öncelik isimle, z16-17'de orta/düşük nokta→isim, z18'de neredeyse hepsi isim.
    - **Declutter** (`map/poi_declutter.dart` · `declutterPois`): ekran-uzayında açgözlü yerleştirme — öncelik sırasına göre, POI **isim uygunsa** (`canLabel`) ve etiket kutusu çakışmıyorsa `label`; değilse **nokta uygunsa** (`canDot`) ve yeterince uzaksa `dot`; hiçbiri olmazsa `hidden`. `canLabel`/`canDot` yukarıdaki zoom eşikleriyle map_screen'de hesaplanır. Deterministik (eşit öncelikte küçük id kazanır → titremez).
    - **Çizim** (`map_screen._buildPoiMarkers`): `_mapController.camera.latLngToScreenOffset` ile POI'ler pikselе projekte edilir, declutter çağrılır; sonuç `PoiMarker` (ikon+isim) veya `PoiDot` (küçük renkli nokta önizleme) olarak çizilir. Her ikisi de `Semantics(button, excludeSemantics)`'li. Seçili POI çakışsa bile isimle gösterilir. Çok yoğun bölgelerde öncelik sırasına göre `_poiDeclutterCap = 700` ile kırpılır. Relayout `MapEventMoveEnd`'de (setState → build) yapılır.
    - **Veri kapısı:** `_poiFetchMinZoom = 15` — bu zoom'un altında POI çekilmez/gösterilmez (şehir ölçeğinde boş harita, kota korunur); üstünde ne gösterileceğine declutter karar verir. `_fetchPoisForVisibleArea` (800ms debounce + cache) ve Foursquare/Overpass çağrı sayısı **değişmedi** (bkz. [[10-Cache-CDN]], [[11-Olcekleme]]).
    - **Venue (Firestore) marker'ları** bilinçli olarak declutter dışında, her zoom'da görünür (ürünün skorlu çekirdek içeriği). **Not:** Marker kümeleme (`flutter_map_marker_cluster`) önce denendi, elendi — kümeler de uzak zoom'da haritayı örtüyordu; declutter daha çok Google'a benziyor.
  - **Monolit bölme (sürüyor):** Sunum parçaları `lib/screens/map/`'e çıkarılıyor — `MapVisuals`, `MapActionButton`, `VenueSheet`, `OsmPoiSheet`, `MapSearchItem`, `UnknownPointSheet`, `SmartResultsOverlay`, `MapTypeCard`, `MapOverlayChip`, `PoiMarker`/`PoiDot`, ve saf birimler `poiPriority`/`declutterPois`. (`MapFilterChip` çıkarılmıştı ama 2026-07-02'de arama çipleriyle birlikte tamamen kaldırıldı — yukarı bkz.) Saf sunum widget'ları widget testli (state mutasyonu/Navigator/fetch map_screen'de thin wrapper'da kalır); `map_screen.dart` 1716→1488 satır. Katman seçici (`_showLayerPicker`) modal + StatefulBuilder ekranda kaldı (yaprak kart/çip görselleri çıkarıldı) (bkz. [[07-CI-CD]] "Widget testleri")
- **Birleşik arama** (`map_search_service.dart`): Nominatim (adres/metin) + Overpass (kategori) paralel, 500ms debounce, koordinat bazlı dedup
  - **Mesafeye göre sıralama — en yakın en üstte (*2026-07-04*):** Arama sonuçları kullanıcının konumuna göre mesafe sıralanır (en yakın mekân listenin en üstünde). `map_screen._onSearchChanged` `debouncedSearch`'e bir **referans konum** geçirir (`userLat`/`userLon`); bu konum hem Overpass kategori aramasının **merkezi** hem de sıralama referansıdır. Sıralama saf/birim-testli `sortResultsByDistance` ile: her sonuca `latlong2 Distance` ile metre cinsi `distanceMeters` eklenir (O(n), kıyaslamada yeniden hesaplanmaz) ve kararlı biçimde artan sıralanır.
    - **Referans konum fallback zinciri (*2026-07-04* düzeltmesi):** Önce `_currentLocation` (GPS); **yoksa** (izin verilmemiş/henüz çözülmemiş) haritanın **baktığı merkez** (`_mapController.camera.center` — görünen alan); harita hazır değilse `_sakaryaCenter`. **Neden:** eskiden yalnız `_currentLocation` geçiliyordu; izin verilmemiş cihazda `null` → `sortResultsByDistance` sıralamayı atlıyor → sonuçlar kategori-öncelikli (Overpass→Nominatim) sırasız kalıyordu. Artık referans **hep dolu** → sıralama her koşulda çalışır. `sortResultsByDistance`'ın saf sözleşmesi korundu (userLat/userLon null ise değişmeden döndürür); değişen yalnız **çağıranın hep bir referans vermesi**. Regresyon testi `test/unit/map_search_sort_test.dart` (bkz. [[07-CI-CD]]). **Erişilebilirlik:** motor/görme engelli kullanıcı en ulaşılabilir (yakın) mekânı önce görür — ürünün varlık sebebiyle örtüşür.
    - **Kategori araması — tüm kategorilere genel çözüm + tek kaynak (*2026-07-04*):** "market" (ve "park" vb.) araması yalnızca **adında** o kelime geçen yerleri getiriyordu, **kategorisi** o olanları değil (BİM/A101 süpermarketleri, isimsiz parklar…). Üç ayrı sorun vardı, üçü birden çözüldü:
      1. **Yanlış OSM tag + drift:** Süpermarket `amenity=supermarket` sanılıyordu; OSM'de **`shop=supermarket`**'tir. Aynı tag hem `categoryFilters` (harita) hem `_turkishQueryToOsmTypes` (arama) tablolarında ayrı tanımlıydı → biri düzeltilse diğeri unutulur. **Çözüm:** iki tablo **tek kaynağa** birleştirildi → `overpass_poi_service.dart` · `PoiCategory` sınıfı + `poiCategories` listesi (her satır: OSM key/value + Türkçe etiket + arama eşanlamlıları + `onMap`). `categoryFilters` (harita, `onMap=true` süzülür) ve `categoriesForQuery` (arama, eşanlamlı substring eşleşmesi — Türkçe ekleri yakalar: "marketler"/"parklar") **buradan türetilir**. Tag artık TEK yerde; drift imkânsız. Ters etiket (`OsmPoi.categoryToTurkish`, FSQ dahil paylaşılan) tüm token'ları kapsar (birim test invariant'ı ile garanti).
      2. **İsim filtresi isimsiz kategori POI'lerini eliyordu:** `searchPoisByCategory` `name.isNotEmpty` ile filtreliyordu; OSM'de park/otopark/tuvalet **sıklıkla isimsiz** → aramada hiç çıkmıyordu. **Çözüm:** kategori aramasında isim filtresi kaldırıldı (yalnız geçersiz koordinat elenir); isimsiz POI'ye başlık `map_search_service`'te `poi.category` (Türkçe kategori adı, ör. "Park"). ⚠️ **Yalnız arama yolunda** — harita katmanı (`fetchPoisForBounds`) isim filtresini korur (görsel yoğunluk artmasın; kullanıcı tercihi *2026-07-04*).
      3. **Kapsam dar:** Eşanlamlı/kategori seti genişletildi (klinik, doktor, dişçi, veteriner, ATM, üniversite, sinema, tiyatro, **tuvalet**, kreş, **AVM**, pansiyon/motel, çocuk parkı, spor merkezi/salonu…). `onMap=false` olanlar aranabilir ama haritayı/kotayı şişirmez.
      - **Etkilenen dosyalar:** `overpass_poi_service.dart` (PoiCategory/poiCategories/categoryFilters/categoriesForQuery, `_turkishQueryToOsmTypes` silindi), `map_search_service.dart` (isimsiz→kategori başlığı), `osm_poi_model.dart` (categoryToTurkish'e toilets/kindergarten/shop_mall). Regresyon: `test/unit/poi_category_search_test.dart` (12 senaryo — market→shop=supermarket+convenience, park, Türkçe ek, onMap türetmesi, token→etiket invariant'ı). Arama listesi ikonu kaynağa (`type`) bağlı olduğundan `map_visuals` değişmedi. Foursquare debounce/cache & harita katman seti (aynı 22 `onMap` kategori) **değişmedi**.
    - **Nominatim konum bias'ı — asıl "en yakın" düzeltmesi (*2026-07-04*):** Sıralama tek başına yetmiyordu: kullanıcı Arifiye'deyken "market" araması listenin en üstünde **İstanbul'daki** bir marketi gösteriyordu. **Kök neden:** Nominatim'e konum bias'ı verilmiyordu → tüm Türkiye'den "önem" sırasına göre dönüyor (hepsi metropol); Overpass ise yalnız **isimli** `amenity=supermarket` döndürüyor (`name.isNotEmpty` filtresi) → yerel isimsiz marketler elenip liste yerelde boş kalıyor; sıralama sadece uzakları sıralayınca "en yakın uzak" (İstanbul) üste geliyordu. Sıralamayı düzeltmek yetmez — **listede yerel sonuç olmalı**. Çözüm: `_searchNominatim`'e referans konum geçilir; sonuçlar önce ~0.5° (≈55 km) **viewbox + `bounded=1`** ile kullanıcı çevresine sınırlanır (uzak metropoller dışlanır). Yerel kutuda hiç sonuç yoksa (ör. başka şehirdeki bir yerin **adı** arandı) **ülke geneli fallback** (`bounded` kapalı) yapılır → uzak adres/yer araması yine çalışır. Overpass merkezi + Nominatim viewbox aynı referansı kullanır; sort yine `sortResultsByDistance`. Böylece yerel arama garanti yerel + en yakın üstte, uzak isimli arama korunur. ⚠️ **Bilinçli sınır:** GPS varken viewbox GPS çevresine kilitlenir (yerel rehber için istenen davranış); uzak bir bölgeyi metinle aramak fallback'e düşer.
  - **Arama overlay'i** (`SmartResultsOverlay`): arama boşken başlık **"Son Aramalar"**, yazarken **"Arama Sonuçları"** (*2026-07-02* — eski "Önerilen Mekanlar" yanıltıcıydı; gösterilen içerik gerçek arama sonucudur). **Son aramalar statik değil** — kullanıcının gerçek geçmişi `SharedPreferences`'tan (`recentMapSearchesParsed`) yüklenir; bir sonuca dokununca `addRecentMapSearch` ile kaydedilir (maks. **15**, dedup — *2026-07-04*'te overlay kaydırılabilir hâle gelince 5→15; klavye kapalıyken daha fazla geçmiş görünür/kaydırılır), boşsa "Henüz arama geçmişi yok". Başlıkta opsiyonel **"Temizle"** eylemi geçmişi siler (`clearRecentMapSearches` → prefs `remove`). Widget testli (`test/widget/smart_results_overlay_test.dart`).
  - **Sesli arama (*2026-07-02*):** Arama çubuğu **boşken** sağda mikrofon (`VoiceSearchButton`), **metin varken** × (temizle). Mikrofona dokununca cihazın **yerleşik OS konuşma tanıyıcısı** (`speech_to_text` → `VoiceSearchService`, `tr_TR`) dinler; tanınan metin arama kutusuna yazılır → mevcut arama akışı (`_onSearchChanged`) kendiliğinden tetiklenir. **Ücretsiz/anahtarsız** — API anahtarı/kota yok, $0 (bkz. [[09-Rate-Limiting]]). **Erişilebilirlik:** motor/görme engelli kullanıcı elle yazmadan arayabilir → ürünün varlık sebebiyle örtüşür; `Semantics` etiketli, widget testli (`test/widget/voice_search_button_test.dart`). Mikrofon izni/KVKK: [[08-Guvenlik]]. *(Dinleme UI'ı 2026-07-03'te küçük "Dinleniyor…" rozetinden tam panele — `VoiceSearchSheet` — taşındı; aşağıya bakın.)*
    - **Hata mesajı düzeltmesi — yanlış izin suçlaması (*2026-07-03*):** Eski `onError` işleyicisi `speech_to_text`'in **gerçek hata kodunu atıp** (`onError: (_)`) HER hatada "Sesli arama başlatılamadı. Mikrofon iznini kontrol edin." gösteriyordu. Ama bu mesaj yalnızca `initialize()` **başarılı** olduğunda (yani izin zaten verili) tetikleniyor; asıl arıza `listen()` sonrası oluşan izinle ilgisiz hatalardı: `error_no_match`/`error_speech_timeout` (kullanıcı konuşmadı), `error_language_unavailable` (cihazda `tr_TR` modeli yok), `error_network`, `error_busy`/`error_client` (bazı Android'lerde başlarken geçici). `cancelOnError: true` ilk hatada oturumu kesiyordu → izin verili olsa da yanıltıcı uyarı. Düzeltme: hata kodunu **saf/birim-testli** `classifyVoiceSearchError` (`lib/utils/voice_search_error.dart`) ile sınıflandır → "mikrofon izni" mesajı YALNIZCA gerçek izin hatasında; konuşulmadı→"Sizi duyamadım, tekrar deneyin", dil yok→"yazarak arayın", ağ/meşgul için doğru mesaj. Yapılandırma zaten doğruydu (Manifest `RECORD_AUDIO` + `android.speech.RecognitionService` `<queries>`, iOS `NSMicrophoneUsageDescription`/`NSSpeechRecognitionUsageDescription` mevcut) — sorun yalnızca Dart hata işlemedeydi. Regresyon testi `test/unit/voice_search_error_test.dart` (bkz. [[07-CI-CD]]); izin/KVKK yönü [[08-Guvenlik]].
    - **Sesli arama paneli — Google tarzı, Aşikar'a özel (*2026-07-03*):** Şikayet: mikrofon çalışıyordu ama süre kısa + kullanıcı **aktif mi anlamıyordu** (küçük "Dinleniyor…" rozeti fark edilmiyordu). Google Haritalar'ın sesli arama tam paneli örnek alınıp Aşikar'a uyarlandı → yeni `lib/screens/map/voice_search_sheet.dart`. Mikrofona dokununca alttan **tam panel** açılır (`showVoiceSearchSheet` → `showModalBottomSheet`, harita arkada kararır): büyük "Aramak istediğiniz yeri söyleyin" başlığı, **nabız atan mikrofon dalga animasyonu** (dinlerken `AppColors.primary` + `secondary` halkalar; durunca soluk `outline`), **canlı (kısmi) tanıma metni**, erişilebilirlik odaklı **hızlı öneri çipleri** (Eczane/Hastane/Market/Restoran/Tuvalet/Otobüs durağı — dokununca o terimle arar), **İptal + Tekrar dene** eylemleri. **Mikrofona dokunuş (2026-07-03):** dinlerken paneldeki mikrofona dokununca dinleme **sonlanır** (`service.stop` → o ana dek tanınanı finalize eder: doluysa arar, boşsa "duyamadım"a düşer); dinleme dışıysa yeniden başlatır — `Semantics(button)` etiketi faz'a göre "Dinlemeyi bitir"/"Yeniden dinlemek için dokunun". **Sese tepkili dalgalar + sesli yönerge (2026-07-03):** (1) Mikrofon halkalarının genliği/parlaklığı **canlı ses seviyesine** göre değişir — `speech_to_text` `listen(onSoundLevelChange:)` ham dB'yi (Android ~ -2..10) 0..1'e normalize edip yumuşatır (`_level` `ValueNotifier`, yalnız mikrofon repaint olur; halka animasyonu `Listenable.merge([controller, level])`). Sessizde küçük/soluk, konuşurken büyük/belirgin → kullanıcı sesinin duyulduğunu görür. (2) Panel açılınca yönerge **sesli okunur** ("Aramak istediğiniz yeri söyleyin") — `TtsService` (`lib/services/tts_service.dart`, `flutter_tts`, cihaz OS TTS'i, **ücretsiz/anahtarsız** $0, `tr-TR`). **Sıra önemli:** önce TTS konuşur, **bitince** dinleme başlar (`awaitSpeakCompletion` → tanıyıcı kendi promptunu duymasın). TTS erişilebilirlik ek konforu → hata sessizce yutulur, akışı bloklamaz. **Yalnız görme engelliye (2026-07-03):** yönerge SADECE görme desteğine ihtiyacı olan kullanıcıda sesli okunur; diğer kullanıcılarda sessiz (panel başlığı yine görünür). Koşul `visualSupportProvider` (`lib/providers/user_providers.dart`) ile: `users/{uid}.accessibilityPrefs` içinde `'Görme Desteği'` var mı (kayıt anketi `accessibility_prefs_screen`). map_screen provider'ı `ref.watch` ile sıcak tutup basışta `ref.read(...).valueOrNull ?? false` değerini `showVoiceSearchSheet(speakPrompt:)`'e geçirir; host `speakPrompt` false ise TTS'i atlayıp doğrudan dinler. Görme engelli değilse veya veri yoksa okuma yok (bkz. [[04-Auth]] · kullanıcı tipi/`accessibilityPrefs`). Android 11+ görünürlük: Manifest `<queries>` `android.intent.action.TTS_SERVICE` (+ mevcut `RecognitionService`). Erişilebilirlik: görme engelli kullanıcı yönergeyi duyar; `AppColors` + `Semantics` korundu. (bkz. [[09-Rate-Limiting]] · $0 sesli katman) Durum satırı `Semantics(liveRegion)` ile ekran okuyucuya duyurulur (Google'ın yüz çizimi yerine erişilebilirlik odaklı sade mikrofon). **Mimari:** `VoiceSearchSheet` **saf sunum** (durum dışarıdan → widget testli `test/widget/voice_search_sheet_test.dart`), oturumu (initialize→listen→sonuç) yürüten `_VoiceSearchSheetHost` özel; panel tanınan metni/seçilen öneriyi **döndürür**, `map_screen` yalnızca arama kutusuna yazar (mantık/sunum ayrımı korundu). **Süre:** `listen` `listenFor` 20→**30sn**, `pauseFor` 3→**5sn** (ağır konuşan/erişilebilirlik kullanıcısı yetişsin). **Bayat-callback düzeltmesi:** `VoiceSearchService.initialize` artık `onStatus`/`onError`'ı **her açılışta günceller** (eski hâlde ikinci açılış ilk -unmount- oturumun callback'ine düşerdi). Hata mesajları yine `classifyVoiceSearchError`'dan (yukarıdaki düzeltme). `AppColors` + `Semantics` zorunlu; Foursquare debounce/cache & arama mantığı değişmedi (bkz. [[07-CI-CD]] · widget/regresyon testleri, [[09-Rate-Limiting]] · $0, [[08-Guvenlik]] · mikrofon izni).
- **Rota / yol tarifi** (`route_screen.dart`): **OSRM** tabanlı (birincil `routing.openstreetmap.de` yaya+araç, yedek `router.project-osrm.org` yalnız araç); GeoJSON geometri, API düşerse düz-çizgi fallback. Mesafe/süre gösterimi, rota ters çevirme/paylaşma
  - **Yol tarifi girişi — Google tarzı hedef seçme ekranı (*2026-07-03*):** Haritada konum butonunun **altında** turkuaz yol tarifi FAB'ı (`Icons.directions`, `AppColors.secondary`, `Semantics('Yol tarifi')`) → `AppRoutes.directions` (`DirectionsSearchScreen`, `lib/screens/directions_search_screen.dart`). Google Haritalar yön tarifi ekranına benzer, Aşikar'a özel: sabit başlangıç **"Konumunuz"** (GPS'i RouteScreen çözer), **"Varış noktası seçin"** metin alanı (autofocus), **ulaşım modu sekmeleri** (Yürüyüş/Tekerlekli Sandalye/Taşıt — RouteScreen `_modes` ile **aynı index**), son aramalar + canlı sonuç listesi. Hedef seçilince son aramalara kaydeder ve `RouteScreen`'e geçer (`destinationName`/`destinationLocation` + yeni `initialModeIndex` → seçilen mod taşınır). Arama/son-arama için map_screen ile **aynı** servisleri yeniden kullanır (`MapSearchService` debounce, `SettingsService.recentMapSearchesParsed`, `MapSearchItem`/`MapVisuals`) — çatallamaz. `AppColors` + `Semantics` zorunlu. FAB dizilimi yukarı kaydı (konum butonu bottom 32→100, katman/yükleniyor 100→168). ⚠️ Tekerlekli sandalye modu hâlâ yaya OSRM profilini kullanır (bkz. aşağıdaki "Rota erişilebilirliği" açık konusu).
    - **Çift yönlü uç + swap (*2026-07-03*):** İlk sürümde başlangıç sabit metindi ve varış TextField'ı dikeyde ortalı değildi. Düzeltmeler: (1) **Her iki uç da düzenlenebilir TextField** — başlangıç varsayılan "Konumunuz" (turkuaz, `_originIsCurrent`), üstüne dokununca metin seçilir ve "Başlangıç noktası seçin" olarak aranır; varış "Varış noktası seçin". Aktif alan (`_active`) odak dinleyicisiyle izlenir; liste aktif alanın sorgusuna göre son aramalar/canlı sonuç gösterir; seçim aktif alanı doldurur, iki uç hazırsa (`_originReady && _destReady`) `RouteScreen`'e geçer, değilse eksik alana odaklanır. "Konumunuz" alanına **dokununca metin temizlenir** (kullanıcı yeni yer yazsın); boş bırakıp çıkılırsa otomatik "Konumunuz"a döner (her zaman geçerli başlangıç). (2) **Dikey ortalama** doğru çözüm: `InputDecoration(contentPadding: EdgeInsets.symmetric(vertical: 14))` — **simetrik** dikey padding metni garantili ortalar. ⚠️ `isDense: true` ve `isCollapsed: true` KULLANMA: ikisi de `textAlignVertical.center`'ı bozup metni üste yaslıyor (önceki iki deneme bu yüzden başarısızdı). Alan yüksekliği contentPadding'den gelir (~48px), `SizedBox` ile sabit yükseklik gerekmez. (3) **↑↓ swap butonu** başlangıç↔varış (metin + `isCurrent` + konum) takas eder. `_suppressSearch` bayrağı programatik metin yazımında dinleyicinin arama tetiklemesini engeller.
    - **Ortak başlangıç/varış kartı — iki ekran aynı görünüm (*2026-07-04*):** DirectionsSearchScreen (iki ayrı kenarlıklı kutu) ile RouteScreen (kompakt tek kart) farklı görünüyordu. Kullanıcı tercihi: **kompakt tek kart** (Google deseni, harita dostu) ama erişilebilirlik için **bir tık daha büyük** (engelli kullanıcı kolay bulsun/dokunsun). Tek ortak widget'a çıkarıldı → `lib/widgets/route_endpoints_card.dart` (`RouteEndpointsCard`): tek beyaz kart, geri oku + başlangıç (turkuaz nokta) / noktalı bağlayıcı / varış (kırmızı pin) + sağda yuvarlak swap (opsiyonel ⋮ menü). Satırlar erişilebilir ama fazla büyük olmayan boyutta (`minHeight 50`, yazı 16, swap 42px — ilk 60/17/48 denemesi çok büyüktü, küçültüldü *2026-07-04*), `Semantics` etiketli. Satır **içeriği** dışarıdan: giriş ekranında düzenlenebilir `TextField` (X temizle), rota ekranında görüntü `Text`. Böylece **görünüm ortak, davranış her ekranda kalır** — biri düzeltilip diğeri unutulamaz (aynı `_SearchResultsContainer` felsefesi). Dikey ortalama simetrik `contentPadding` ile (isDense/isCollapsed KULLANMA). RouteScreen'in eski `_buildLocationBox` + inline kartı, DirectionsSearchScreen'in `_buildFieldRow`/`_fieldDecoration`/`_DottedConnector`'ı kaldırıldı.
    - **RouteScreen'de gerçek swap + özel başlangıç (*2026-07-03*):** RouteScreen artık başlangıç/varış'ı **simetrik** tutar: `_startLoc`/`_endLoc` `null` ise o uç **anlık konumdur** (GPS). Böylece (a) yön ekranından özel başlangıç (`startName`/`startLocation`) geçirilebilir, (b) ↑↓ butonu (önceden yalnız snackbar gösteren **sahte** buton) artık uçları takas edip rotayı **yeniden hesaplar** (`_swapEndpoints` → `_fetchRoute`); takas sonrası varış "Konumunuz" olabilir (null → GPS). Marker/konum kutuları `_effectiveStart`/`_effectiveEnd` ve `_startName`/`_endName` kullanır. Constructor geriye uyumlu (`destinationLocation` artık nullable; mevcut venue/POI çağrıları gerçek konum geçtiği için etkilenmez).
    - **RouteScreen'de konuma dokununca düzenleme (*2026-07-04*):** RouteScreen'deki başlangıç/varış satırlarına dokununca (`RouteEndpointsCard.onOriginTap`/`onDestTap`) `DirectionsSearchScreen` **düzenleme modunda** açılır: mevcut uçlar seed olarak taşınır (`initialOrigin*`/`initialDest*`), dokunulan alan odaklanır (`focusField`), gerçek yer varsa metin seçili gelir (kolay değiştir). Seçim yapılınca tekrar RouteScreen'e döner. Navigasyon `pushReplacementNamed` ile (route↔directions döngüsü **düz** kalır, yığılmaz). Router `AppRoutes.directions` artık seed argümanlarını geçer (argümansız FAB girişi de çalışır).
- CallScreen (Agora), mekân ekleme/detay. **Çağrı kabulü** (`notification_service`): kanal adı yalnızca event'ten okunur (`validChannelName`, saf doğrulama → `lib/utils/channel_validator.dart`, birim testiyle korunur — bkz. [[07-CI-CD]]); geçersizse çağrı **kabul edilmez** — paylaşılan sabite fallback yok (yanlış görüşme önlenir, bkz. [[02-API-Arka-Uc]])
- **Çağrı zaman aşımı (arayan):** `CallScreen` 45 sn içinde gönüllü bulamazsa transaction ile `zaman_asimi` yazar ve "Şu anda uygun bir gönüllü bulunamadı" + "Tekrar Dene" ekranını gösterir (`Semantics(liveRegion: true)` ile ekran okuyucu anında seslendirir). Gönüllü tam zamanında üstlenirse görüşme normal sürer (bkz. [[02-API-Arka-Uc]], [[03-Veritabani]])
- Splash + tam kayıt akışı

**YOK:**
- Offline-first / yerel veri senkronu (sadece `SharedPreferences` ayarları kalıcı)
- Animasyon/onboarding turu
- Tablet/responsive özel layout
- Web'e özel uyarlama (mevcut layout web'de "çalışır ama optimize değil")

## Açık Sorular
- `TextScaler` 1.4x'te en uzun butonlar taşıyor mu? (overflow testi yapılmadı)
- Görme engelli kullanıcı haritayı nasıl kullanacak? Harita doğası gereği görsel — sesli alternatif (liste modu) gerekli mi?
- Web'de Agora/CallKit davranışı doğrulanmadı; web "ikincil" derken çağrı dahil mi?
- **Rota erişilebilirliği:** `route_screen` OSRM'in yaya/araç profillerini kullanıyor; **tekerlekli sandalyeye özel profil yok**. Engelli birey için "erişilebilir rota" (rampa/kaldırım dikkate alan) gerçek anlamda sunulmuyor — ürünün varlık sebebiyle çelişir mi? (bkz. [[PRD]])

## TODO
- [~] En büyük dosya `map_screen.dart` bölme (**2603 → 1458 satır, −%44**) — `lib/screens/map/` klasörü; çıkarılan saf sunum widget'ları artık widget testli (bkz. [[07-CI-CD]]):
  - **Aşama 1 ✅:** durumsuz görsel eşleyiciler + `mergePois` → `map_visuals.dart` (`MapVisuals`); tekrar eden iki renk fonksiyonu birleştirildi; POI paleti `AppColors`'a taşındı.
  - **Aşama 2 ✅:** OSM POI detay paneli → `osm_poi_sheet.dart` (`OsmPoiSheet`, `onClose` callback'i ile durumsuz); paylaşılan aksiyon butonu → `map_action_button.dart` (`MapActionButton`, venue + POI sheet ortak).
  - **Aşama 3 ✅:** DB mekan detay paneli + yorum kutucuğu → `venue_sheet.dart` (`VenueSheet`, `onClose` ile durumsuz).
  - **Aşama 4 ✅ (Slice 1):** saf sunum çipi/satırı → `map_filter_chip.dart` (`MapFilterChip`) *(2026-07-02'de kaldırıldı — arama çipleri harita filtreleme modalıyla tekrar ediyordu)*, `map_search_item.dart` (`MapSearchItem`); state mutasyonu thin wrapper'da.
  - **Aşama 5 ✅ (Slice 2):** bilinmeyen nokta sheet'i → `unknown_point_sheet.dart` (`UnknownPointSheet`); akıllı arama overlay'i → `smart_results_overlay.dart` (`SmartResultsOverlay`); `_buildSearchItem` wrapper'ı `_onSearchItemTapped`'e indi.
  - **Aşama 6 ✅ (Slice 3):** katman seçici yaprak görselleri → `map_type_card.dart` (`MapTypeCard`), `map_overlay_chip.dart` (`MapOverlayChip`); `_showLayerPicker` modal + StatefulBuilder ekranda kaldı.
  - **Aşama 7 ✅ (Slice 4 — kısmi):** Overpass **sorgu/bbox üreteçleri** saf dosyaya çıkarıldı → `lib/services/overpass_query_builder.dart` (`overpassBoundingBox`, `hikingOverpassQuery`, `accessibilityOverpassQuery`), birim testli (`test/unit/overpass_query_builder_test.dart`); tekrarlanan bbox kodu birleşti. **Bilinçli ekranda kalan:** HTTP çağrısı + JSON→UI (Polyline/Marker) eşlemesi + setState (`_fetchHikingLayer`/`_fetchOverpassLayer`/`_fetchPoisForVisibleArea`) — koruyucu test yok, UI-kuplajlı (bkz. [[10-Cache-CDN]] · debounce/cache).
- [x] **Arama overlay yüksekliği — içerik kadar + tavan (*2026-07-03*):** `SmartResultsOverlay` (harita arama paneli) her iki durumda da — **(a)** "Son Aramalar" geçmişi, **(b)** yazdıkça gelen canlı "Arama Sonuçları" — beyaz arka planı boş alanda uzatıp haritayı örtüyordu. Neden: `map_screen`'deki `Positioned`'da `bottom: 16` dikey kısıtı "tam ekran"a sabitliyor, widget içindeki `Expanded` de o yüksekliği dolduruyordu. Çözüm: `Positioned`'dan `bottom` kaldırıldı (dikey kısıt gevşedi) + yükseklik davranışı **tek ortak sarmalayıcıya** çıkarıldı → `_SearchResultsContainer` (private, `smart_results_overlay.dart` içinde): `Column(mainAxisSize: min)` (yükseklik = başlık + içerik kadar) + `ConstrainedBox(maxHeight: ekran × 0.40)` (15-20 öğe gelse bile ekranı kaplamaz) + gövde `Flexible` içinde `ListView.separated(shrinkWrap: true)` (az öğe = az yer; tavana değince **kendi içinde** kayar, dıştaki harita kaymaz). Boş geçmiş bilgilendirmesi `Expanded` yerine sabit `SizedBox(height: 160)`. **Ortak sarmalayıcı bilinçli:** iki durum aynı widget'tan geçtiği için biri düzeltilip diğeri unutulamaz. Yalnızca sunum/layout — Foursquare debounce/cache ve arama mantığı (bkz. [[10-Cache-CDN]]) **değişmedi**; `AppColors` + "Temizle"/liste `Semantics` etiketleri korundu (bkz. [[07-CI-CD]] · `smart_results_overlay_test` 7/7 yeşil, `flutter analyze` temiz).
  - **Klavye durumuna duyarlı tavan (*2026-07-03*):** Sabit %40 tavan, klavye açık/kapalı fark etmiyordu → klavye kapanınca panel genişlemiyor, 4+ sonuç hâlâ dar alanda kaydırılıyordu. `_SearchResultsContainer.maxHeight` artık `MediaQuery`'den hesaplanır: `ekran − (padding.top+96 üst rezerv) − viewInsets.bottom (klavye) − 24`, `%60` ile tavanlanır. Klavye **açıkken** `viewInsets.bottom` büyür → tavan daralır (≈4 öğe + kaydırma); **kapanınca** büyür → panel öğe sayısı kadar uzar (daha fazla sonuç görünür). `MediaQuery` bağımlılığı sayesinde klavye açılıp kapanınca otomatik yeniden hesaplanır. Harita hep görünür (%60 sınırı).
  - **Gerçek gövde yüksekliğine geçiş — alttaki boşluğu doldur (*2026-07-04*):** Şikayet: arama çubuğuna dokununca sonuçlar 5 gösterip kesiliyordu, klavye **kapalıyken** altta **boş harita** kalıyor, panel oraya uzamıyordu. Neden: eski tavan `MediaQuery.size.height`'i (tüm ekran, **alt navigasyon çubuğu 85px dahil**) baz alıp `%60` ile sınırlıyordu → panel gövdenin ortasında tıkanıyor, `%60` bağlayıcı kısıt oluyordu; ayrıca `MapScreen` **kendi Scaffold'unda** olduğundan `MediaQuery` gövdenin gerçek yüksekliğini vermiyordu. Çözüm: `MapScreen` body `Stack`'i **`LayoutBuilder`** ile sarıldı → `constraints.maxHeight` = alt nav çubuğunun **üstündeki** gerçek gövde yüksekliği (Scaffold klavye açılınca burayı küçülttüğü için **klavye zaten dışlanır**). Bu değer `_mapBodyHeight` alanına yazılıp `SmartResultsOverlay.availableHeight`'e geçirilir; `_SearchResultsContainer` tavanı `availableHeight − (padding.top+96) − 12` yapar (yoksa test/fallback `MediaQuery`). Sonuç: **%60 tavanı kalktı** — klavye **kapalıyken** panel alttaki boşluğa (nav çubuğunun hemen üstüne) kadar uzar, klavye açılınca klavyenin üstünde kalır; her iki durumda taşan liste kendi içinde kayar (dıştaki harita kaymaz). `Positioned`'a **neden bottom verilmedi:** verilirse panel içeriği olmasa da tüm alanı kaplar; `mainAxisSize.min` ile az öğede kompakt kalır. **Son aramalar 5→15** ([[#Birleşik arama]] · `addRecentMapSearch`) çünkü artık kaydırılabilir alan var. Yalnız sunum/layout — Foursquare debounce/cache & arama mantığı **değişmedi**; `AppColors` + `Semantics` korundu. Regresyon: `smart_results_overlay_test` kaydırma + geniş-alan senaryolarıyla **9/9** yeşil, `flutter analyze` temiz (bkz. [[07-CI-CD]]).
- [x] **Inline renk temizliği (O2 — *2026-06-29*):** `lib/` genelindeki 27 inline `Color(0xFF...)` (map_screen 16, route_screen 4, settings_provider 3 HC, registration_complete 2, register 1, profile 1) `AppColors`'a taşındı — aynı hex korunarak (davranış değişmedi). Eklenen sabitler: harita katman/overlay paleti (`mapSteps`/`mapFootway`/`mapWheelchair`/`mapElevator`/`mapType*`/`mapTransit`/`mapCycling`), rota modu (`routeWalk`/`routeWheelchair`/`routeTransit`), `divider`/`success*`/`error`, yüksek kontrast (`hcYellow`/`hcNavy`). `flutter analyze` temiz, 62 test yeşil. Artık `lib/`'de `AppColors` dışında inline renk YOK (CLAUDE.md "Asla Yapma" #3 sağlandı).
- [ ] Tüm kritik akışları TalkBack + VoiceOver ile elle test et
- [ ] `font_scale` 1.4x için overflow regresyon testi
- [ ] Harita için "liste görünümü" erişilebilir alternatifini değerlendir → [[PRD]]'ye ekle

---

## İlgili Notlar
- [[Architecture-Overview]] — Frontend'in sistemdeki yeri
- [[Vision]] — erişilebilirlik vizyonunun temas noktası
- [[PRD]] — ekranların gereksinim kaynağı
- [[02-API-Arka-Uc]] — çağrı/token için backend
- [[03-Veritabani]] — realtime veri + mekân kaynağı
- [[04-Auth]] — giriş ve kullanıcı tipi yönlendirmesi
- [[05-Barindirma]] — uygulamanın dağıtım hedefi
- [[10-Cache-CDN]] — POI/tile/arama önbelleği
